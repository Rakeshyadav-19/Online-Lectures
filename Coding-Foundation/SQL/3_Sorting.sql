SELECT *
FROM DB
-- Data is sorted by fields.
ORDER BY fields

-- Your extracted data will be sorted in ascending order by default. If you need the data sorted in descending order (from largest to smallest) you need to add the DESC keyword. 
ORDER BY field DESC